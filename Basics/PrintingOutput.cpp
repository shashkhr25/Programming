#include<stdio.h>
int main(){
	
	printf("Life is beautiful!!! \n");
	printf("109 \n");
	//Everything in "" is a string.
	
	int x = 110,y=9;
	float f = 7.2;
	printf("Value of x is : %d , y is : %d , the float is : %.1f", x,y,f );
	//"%d" is a Format/Type specifier, used to specify the type of variable being printed.
	// we cannot use a function accorfing to out will.
	// %d : integer, %f : float, %l : long, %ll/ %I64d : long long
	
	return 0;
}
